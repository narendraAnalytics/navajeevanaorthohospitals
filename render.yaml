services:
  - name: orthoai-backend
    type: web
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python scripts/seed_knowledge_base.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: INFO
      - key: CHROMA_MODE
        value: local
      - key: ALLOWED_ORIGIN
        value: http://localhost:3000
      - key: GROQ_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
      - key: NEON_DB_URL
        sync: false
