Duration 0:00 — changed to preload="auto" + added onDurationChange handler + useEffect that reads duration on mount (catches the
  cached-video case where onLoadedMetadata never re-fires).

  Timeline click — replaced the invisible <input type="range"> with a onMouseDown → getBoundingClientRect() → seek pattern directly on the
  track div. Click seeks precisely to that position AND auto-starts playback. Dragging along the timeline updates position live. The track
  hit area is now 20px tall so it's easy to click.